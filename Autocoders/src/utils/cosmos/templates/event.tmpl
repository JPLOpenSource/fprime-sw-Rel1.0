# Created on ${date}
# Author: ${user}
#
# THIS FILE IS AUTOMATICALLY GENERATED - DO NOT EDIT!!!
#
# XML Source: ${source}

# COMPONENT: "${component_string}"
TELEMETRY ${target_caps} ${channel_name} ${endianness} "${chn_desc}"
    #raw<%=#end raw render "_ref_tlm_evr_hdr.txt", locals: {id: ${id}} %>
    APPEND_ITEM ARG1 32 INT "Arg1"
    ITEM MESSAGE 0 0 DERIVED "Formatted String for Argument"
      GENERIC_READ_CONVERSION_START STRING 0
        sprintf('${format_string}'#for $name in $arg_names , packet.read('$name') #end for #if not $last_arg == "NONE" packet.read('$last_arg')#end if)
      GENERIC_READ_CONVERSION_END
    ITEM EVR_SEVERITY 0 0 DERIVED "Severity"
      GENERIC_READ_CONVERSION_START STRING 0
        sprintf('${severity}')
      GENERIC_READ_CONVERSION_END